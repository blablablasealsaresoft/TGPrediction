# CI/CD Requirements
# Subset of dependencies for automated testing environments

# Core testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
pytest-cov==4.1.0

# Code quality
black==23.12.1
flake8==7.0.0
mypy==1.8.0

# Base dependencies (lightweight)
python-dotenv==1.0.0
aiohttp==3.9.1
asyncio==3.4.3

# Database (SQLite only for CI)
sqlalchemy==2.0.23
aiosqlite==0.19.0

# Telegram
python-telegram-bot==20.7

# Mocking Solana deps (don't install actual Solana libs in CI)
# Instead, use pytest-mock to mock solana.rpc.async_api.AsyncClient
# and solders.* modules

# Note: For full integration tests, use requirements.txt
# This file is for fast unit tests and linting only

